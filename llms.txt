# fethr

> fethr is a featherweight code editor with first-class AI agent integration,
> currently under construction. The name is pronounced "feather".

## What it is

- A minimal, fast desktop code editor where an AI coding agent is a first-class
  peer process, not a bolted-on extension: the editor forwards context
  (selection, files, project) and renders results (streams, diffs); the
  intelligence lives in the agent runtime.
- Designed around the front-end/runtime split popularized by Claude Code.
  fethr is an independent project, not affiliated with or endorsed by Anthropic.
- No extension platform by design: language intelligence via LSP, syntax via
  tree-sitter, agent via its own protocol.

## Status

Alpha. `npx @evojewel/fethr@alpha .` opens a working editor with an agent panel: a local
server bound to 127.0.0.1 with the OS browser as the shell, CodeMirror 6 core, file tree,
syntax highlighting (JS/TS, Python, Markdown, HTML, CSS, JSON), edit and save. No Electron.
File access is confined to the launch directory.

The agent panel (Cmd-J) runs on the Claude Agent SDK using the machine's existing Claude
Code login. It is read-only by construction (Read/Grep/Glob only; all mutating tools
disabled); its sole change mechanism is a propose_edit tool whose proposals render as diff
cards the writer accepts or rejects — accepted edits apply in the editor, never directly
to disk. The panel shows live agent state (thinking/tool/writing phases, streamed thinking
in a collapsible block, per-tool activity detail), has a stop button, a model picker
(default/opus/sonnet/haiku/fable 5 — fable folds back to default if unavailable on the
account), renders markdown replies, and an auto-apply mode that skips the manual Accept
click while still requiring an explicit save before anything hits disk. Cmd-B collapses
the file-tree sidebar. A context pill row above the composer shows exactly what's being
sent — current file, live selection, and any files attached via @-mention (autocomplete
against the workspace). File:line references in replies (e.g. app.js:42) render as
clickable links that jump the editor there. A mic button does live speech-to-text via the
browser's Web Speech API for hands-free prompting.

A native macOS app shell (Tauri, src-tauri/) provides the same editor including the full
agent panel: it bundles the Node server as a sidecar and spawns it on launch (needs a
system Node install; the app locates Homebrew/nvm installs automatically). Bundle size
is ~360MB due to the Agent SDK. `npx @evojewel/fethr` stays the lightweight option.

## Links

- Source: https://github.com/evojewel/fethr
- npm: https://www.npmjs.com/package/@evojewel/fethr (command installed: `fethr`)
- Author: Jewel Lukose
